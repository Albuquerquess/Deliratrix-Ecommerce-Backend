export interface debtorProps {
    name: string;
    email: string;
    phone: string;
}

export interface finalContentsUrlProps {
    create_id: string;
    url: string;
}

export interface contentProps {
    content: string;
    price: number;
    txid: string
}